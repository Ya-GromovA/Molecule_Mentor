<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Custom AndroidManifest для Molecule Mentor
    Отключает hardware acceleration чтобы избежать краша:
    "pthread_mutex_lock called on a destroyed mutex" в HWUI
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.moleculementor.moleculementor">
    
    <!-- Hardware acceleration отключен на уровне application -->
    <application
        android:hardwareAccelerated="false"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher">
        
        <!-- Главная SDL Activity с отключенным hardware acceleration -->
        <activity
            android:name="org.kivy.android.PythonActivity"
            android:hardwareAccelerated="false"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|fontScale|uiMode|screenSize|smallestScreenSize|layoutDirection|density"
            android:screenOrientation="portrait"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>
    
</manifest>
